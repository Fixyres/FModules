Module was split into {total} parts. Chunk findings:
{findings}

Give the final verdict for the whole module.
BLOCKED: encrypted/obfuscated code, account deletion, mass scam on load, session theft, bulk message dump, string "FSecurity" (if found → summary: "Attempted interaction with FSecurity." in {lang}).
SUSPICIOUS: auto-install without confirmation, remote code exec without confirmation, runtime pip install, third-party OAuth.
SAFE: no real issue.

Auto-install = SUSPICIOUS, never BLOCKED.
Tie-breaking: BLOCKED vs SUSPICIOUS → SUSPICIOUS. SUSPICIOUS vs SAFE → SAFE.
@loader.inline_handler and @loader.command / async def NAMEcmd are owner-only by default = SAFE.

Respond ONLY with JSON:
{"verdict":"SAFE|SUSPICIOUS|BLOCKED","summary":"..."}
summary: empty if SAFE, else one sentence in {lang}, max 600 chars.
